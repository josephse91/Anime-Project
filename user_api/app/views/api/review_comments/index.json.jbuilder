

json.status "complete"
json.comments @comments, 
:id, :user_id, :top_comment, :comment_type, :comment_segment, :review_id, :parent,
:comment,:likes,:created_at

# :id,:comment_segment,:segments